#!/bin/bash
# Symphony-CC CLI wrapper
# 프로젝트 루트에서 lib.cli 모듈을 실행한다.

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_ROOT" || exit 1
exec python3 -c "from lib.cli import main; exit(main())" "$@"
